# QA session results

> **This file keeps this name from now until it merges — there is nothing to rename.**
> Nothing here is published until you run the Submit button:
>
> ```
> python3 Tools/QA/submit.py
> ```
>
> It checks the required fields, refuses while anything is missing, tells you exactly
> what to fix, and only then publishes. Add rows across as many days as you like and
> run it again each time — only the new verdicts are published. An already-published
> verdict is **frozen**: a retest is a NEW session file, never an edit.
>
> `Result` must be exactly `PASS` / `FAIL` / `PARTIAL` / `BLOCKED` / `SKIP`. Never
> delete the `qa-results-table` markers — they are what stops the scratch notes below
> from being read as verdicts. Full guide: `Docs/QA/README.md`.

## Session

| Field | Value |
|---|---|
| Tester | Caleb |
| Date | 2026-08-14 |
| Branch | claude/qa-backlog-7mvlsr |
| Commit | `5144ad269` |
| Unity version | 6000.3.17f1 |
| Platform(s) | Editor (macOS) |
| Submitted | yes — 2026-08-25 10:03 · 1 verdict(s), 0 new this run |
## Results

<!-- qa-results-table -->

| ID | Result | Notes |
|---|---|---|
| QA-BUILD-COMPILE | PASS |  |

<!-- /qa-results-table -->

---

## Working notes for QA-BUILD-COMPILE

*(Scratch space — the script ignores everything outside the table above. Fill this in
as you go; it becomes the dev task's "Observed" text if the item fails.)*

**The item:** the project compiles, imports and boots. ~30 branches of hand-authored
C#, prefab, scene and ScriptableObject YAML have never been through Unity's importer
or compiler.

### Step 1 — open the project on this branch, let import + compile settle

- Time to settle: ______
- Did the importer run a full reimport, or a warm `Library/`? ______

### Step 2 — read the whole Console

Paste **verbatim**. The first compile error is the one that matters; everything else
on the backlog is blocked behind it.

```
(compile errors here — full text, including file:line)
```

```
(Missing (Mono Script) rows — which object, which prefab/scene)
```

```
(broken/dangling reference + meta regeneration warnings)
```

**Known, do NOT record as failures** (pre-existing, already logged):
- `SerpentHUDVariant.prefab` and `VesselHUDPrefab.prefab` carry a missing script,
  guid `57dc27a3f7264d548b51007c0615f701`.
- Manta and Serpent carry dangling package `m_Script` guids (unresolvable without
  `Library/PackageCache`).

### Step 3 — boot path

| Stage | Reached? | Exception (verbatim) |
|---|---|---|
| `Menu_Main` |  |  |
| Enter freestyle |  |  |
| Return to menu |  |  |
| Launch one arcade game (which: ______) |  |  |
| Return home |  |  |

### Verdict

- **PASS** — zero compile errors, no new missing scripts, and all five boot stages
  clean.
- **FAIL** — any compile error, any new missing script, or an exception that stops
  the boot or the round.
- **BLOCKED** — you could not get far enough to judge (say what stopped you).

## Evidence

*(Screenshots, console dumps, clips — reference them by item ID.)*

## Anything else

*(Feel, tuning opinions, and anything that looked wrong but is not on the list. These
change no item's status, but they are read when the backlog is regenerated and can
become new items.)*
