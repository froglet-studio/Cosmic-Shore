using UnityEngine;

namespace CosmicShore.UI
{
    /// <summary>
    /// Profile screen in the menu.
    /// Profile display (name + avatar) is handled by a ProfileDisplayWidget component
    /// on the same or child GameObject — no code needed here for that.
    /// Extend this class if the profile screen needs screen-specific logic beyond display.
    /// </summary>
    public class ProfileScreen : MonoBehaviour
    {
    }
}
